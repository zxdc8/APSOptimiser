function [obj con]=ObjConWrapper(X)


    key=round(1000000*rand)
    
    J=objectivepso(X,key)

    [C Ceq]=constraintspso(X,key)

    %Evaluate constraints and subtract to objective function if needed.
    
    
    
end